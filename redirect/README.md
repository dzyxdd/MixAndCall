# Legacy host redirects

These stubs keep old bookmarks working:

- GitHub Pages: `https://dzyxdd.github.io/MixAndCall/` → strips `/MixAndCall`
- GitLab Pages: `https://dzyxdd.gitlab.io/mixandcall/` → strips `/mixandcall`

Canonical origin is currently `https://mixandcall.pages.dev`.
After binding a custom domain, change it in these HTML files (e.g. back to `https://dzyxdd.io`).
